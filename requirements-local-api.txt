# Recommended install for auto360Cut when using a local / remote OpenAI-compatible API.
#
# This pulls in the editable sentrysearch package plus the dependencies needed
# for the local-api backend used by the auto360Cut workflow.
-r requirements.txt
-e ./sentrysearch[local-api]
